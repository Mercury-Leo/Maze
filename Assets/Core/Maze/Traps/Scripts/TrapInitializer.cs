using UnityEngine;

namespace Core.Maze.Traps
{
    public class TrapInitializer : MonoBehaviour
    {

        [SerializeField] private LogicalTrap _trap;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
