using UnityEngine;

namespace View.Input.Scripts
{
    
    /// <summary>
    /// Doesnt work so far Don't Use
    /// </summary>
    /*public class InputManager : MonoBehaviour
    {
        public static Player PlayerInput
        {
            get => PlayerInput;
            private set => PlayerInput = value;
        }

        private void Awake()
        {
            PlayerInput = new Player();
        }
    
        private void OnEnable()
        {
            PlayerInput.Enable();
        }

        private void OnDisable()
        {
            PlayerInput.Disable();
        }
    }*/
}
